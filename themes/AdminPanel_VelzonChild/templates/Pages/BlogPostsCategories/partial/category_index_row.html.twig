<tr data-node-id="{{ item.id }}" {% if parentId %}data-node-pid="{{ parentId }}"{% endif %}>
    <td>{{ item.name }}</td>
    <td>
    	<a href="{{ path('vsorg_blogposts_categories_update', {'id': item.id}) }}?id={{ item.id }}">Edit</a>
    </td>
</tr>


